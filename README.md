# arch-pkgs

Personal Arch package repository for locally maintained packages.

## Layout

- `packages/<name>/` contains each package's build files and supporting assets.

## Usage

Build and install a package from its directory with `makepkg -si`.
