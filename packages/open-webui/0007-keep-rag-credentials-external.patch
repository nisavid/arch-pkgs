# Keep package-delivered RAG credentials outside persistent application config.
# Open WebUI commit: f9590b8017199e56d5e953657e6498e3cef1d246
# Open WebUI 0.11.0 sdist SHA-256: e28c4fa997bf0a678caa7a0db6441da2e0c33b9a4120677f959ec3e45fccf9e9
# Apply after 0005-require-qualified-reranking.patch and 0006-enforce-session-epoch.patch.
diff --git a/backend/open_webui/models/config.py b/backend/open_webui/models/config.py
index 6081214..ddbdc0b 100644
--- a/backend/open_webui/models/config.py
+++ b/backend/open_webui/models/config.py
@@ -21,6 +21,12 @@ from sqlalchemy import JSON, BigInteger, Column, Text, delete, select
 log = logging.getLogger(__name__)
 
 API_CONFIG_KEYS = ('openai.api_configs', 'ollama.api_configs')
+EXTERNAL_AUTHORITY_CONFIG_KEYS = frozenset(
+    {
+        'rag.openai.api_key',
+        'rag.external_reranker_api_key',
+    }
+)
 DICT_CONFIG_KEY_ALIASES = {
     'openai.api_configs': ('OPENAI_API_CONFIGS',),
     'ollama.api_configs': ('OLLAMA_API_CONFIGS',),
@@ -129,6 +135,8 @@ class Config(Base):
 
     @classmethod
     def persistent_enabled_for(cls, key: str) -> bool:
+        if key in EXTERNAL_AUTHORITY_CONFIG_KEYS:
+            return False
         if not cls.PERSISTENT_ENABLED:
             return False
         if key.startswith('oauth.') and not cls.OAUTH_PERSISTENT_ENABLED:
@@ -170,13 +178,19 @@ class Config(Base):
         default_values = {
             key: value
             for key, value in Config.DEFAULTS.items()
-            if key.startswith(f'{namespace}.') and not Config.persistent_enabled_for(key)
+            if key.startswith(f'{namespace}.')
+            and key not in EXTERNAL_AUTHORITY_CONFIG_KEYS
+            and not Config.persistent_enabled_for(key)
         }
         if not Config.PERSISTENT_ENABLED:
             return default_values
         async with get_async_db() as db:
             result = await db.execute(select(Config).where(Config.key.like(f'{namespace}.%')))
-            values = {row.key: row.value for row in result.scalars().all()}
+            values = {
+                row.key: row.value
+                for row in result.scalars().all()
+                if row.key not in EXTERNAL_AUTHORITY_CONFIG_KEYS
+            }
             values.update(default_values)
             return values
 
@@ -184,10 +198,18 @@ class Config(Base):
     async def get_all() -> dict:
         """Get all config as {key: value}."""
         if not Config.PERSISTENT_ENABLED:
-            return dict(Config.DEFAULTS)
+            return {
+                key: value
+                for key, value in Config.DEFAULTS.items()
+                if key not in EXTERNAL_AUTHORITY_CONFIG_KEYS
+            }
         async with get_async_db() as db:
             result = await db.execute(select(Config))
-            values = {row.key: row.value for row in result.scalars().all()}
+            values = {
+                row.key: row.value
+                for row in result.scalars().all()
+                if row.key not in EXTERNAL_AUTHORITY_CONFIG_KEYS
+            }
             if not Config.OAUTH_PERSISTENT_ENABLED:
                 values.update({key: value for key, value in Config.DEFAULTS.items() if key.startswith('oauth.')})
             return values
@@ -197,6 +219,8 @@ class Config(Base):
         """Upsert multiple config key-value pairs. Raises on failure."""
         persistent_updates = {}
         for key, value in updates.items():
+            if key in EXTERNAL_AUTHORITY_CONFIG_KEYS:
+                continue
             value = _json_value(value)
             if Config.persistent_enabled_for(key):
                 persistent_updates[key] = value
@@ -243,6 +267,9 @@ class Config(Base):
         Existing DB values take precedence over defaults.
         """
         async with get_async_db() as db:
+            await db.execute(
+                delete(Config).where(Config.key.in_(EXTERNAL_AUTHORITY_CONFIG_KEYS))
+            )
             result = await db.execute(select(Config.key))
             existing_keys = {row[0] for row in result.all()}
 
@@ -259,8 +286,8 @@ class Config(Base):
                     existing_keys.add(key)
                     new_count += 1
 
+            await db.commit()
             if new_count:
-                await db.commit()
                 log.info('Seeded %d new config defaults', new_count)
 
     @staticmethod
diff --git a/backend/open_webui/routers/retrieval.py b/backend/open_webui/routers/retrieval.py
index ac7fe75..b4c3b3b 100644
--- a/backend/open_webui/routers/retrieval.py
+++ b/backend/open_webui/routers/retrieval.py
@@ -488,7 +488,6 @@ async def get_embedding_config(request: Request, user=Depends(get_admin_user)):
         'RAG_EMBEDDING_CONCURRENT_REQUESTS': config.RAG_EMBEDDING_CONCURRENT_REQUESTS,
         'openai_config': {
             'url': config.RAG_OPENAI_API_BASE_URL,
-            'key': config.RAG_OPENAI_API_KEY,
         },
         'ollama_config': {
             'url': config.RAG_OLLAMA_BASE_URL,
@@ -504,7 +503,6 @@ async def get_embedding_config(request: Request, user=Depends(get_admin_user)):
 
 class OpenAIConfigForm(BaseModel):
     url: str
-    key: str
 
 
 class OllamaConfigForm(BaseModel):
@@ -564,7 +562,6 @@ async def update_embedding_config(request: Request, form_data: EmbeddingModelUpd
         ]:
             if form_data.openai_config is not None:
                 config.RAG_OPENAI_API_BASE_URL = form_data.openai_config.url
-                config.RAG_OPENAI_API_KEY = form_data.openai_config.key
 
             if form_data.ollama_config is not None:
                 config.RAG_OLLAMA_BASE_URL = form_data.ollama_config.url
@@ -620,7 +617,6 @@ async def update_embedding_config(request: Request, form_data: EmbeddingModelUpd
             'RAG_EMBEDDING_CONCURRENT_REQUESTS': config.RAG_EMBEDDING_CONCURRENT_REQUESTS,
             'openai_config': {
                 'url': config.RAG_OPENAI_API_BASE_URL,
-                'key': config.RAG_OPENAI_API_KEY,
             },
             'ollama_config': {
                 'url': config.RAG_OLLAMA_BASE_URL,
@@ -700,7 +696,6 @@ async def get_rag_config(request: Request, user=Depends(get_admin_user)):
         'RAG_RERANKING_ENGINE': config.RAG_RERANKING_ENGINE,
         'RAG_RERANKING_BATCH_SIZE': config.RAG_RERANKING_BATCH_SIZE,
         'RAG_EXTERNAL_RERANKER_URL': config.RAG_EXTERNAL_RERANKER_URL,
-        'RAG_EXTERNAL_RERANKER_API_KEY': config.RAG_EXTERNAL_RERANKER_API_KEY,
         'RAG_EXTERNAL_RERANKER_TIMEOUT': config.RAG_EXTERNAL_RERANKER_TIMEOUT,
         # Chunking settings
         'TEXT_SPLITTER': config.TEXT_SPLITTER,
@@ -937,7 +932,6 @@ class ConfigForm(BaseModel):
     RAG_RERANKING_ENGINE: str | None = None
     RAG_RERANKING_BATCH_SIZE: int | None = None
     RAG_EXTERNAL_RERANKER_URL: str | None = None
-    RAG_EXTERNAL_RERANKER_API_KEY: str | None = None
     RAG_EXTERNAL_RERANKER_TIMEOUT: str | None = None
 
     # Chunking settings
@@ -1170,12 +1164,6 @@ async def update_rag_config(request: Request, form_data: ConfigForm, user=Depend
         else config.RAG_EXTERNAL_RERANKER_URL
     )
 
-    config.RAG_EXTERNAL_RERANKER_API_KEY = (
-        form_data.RAG_EXTERNAL_RERANKER_API_KEY
-        if form_data.RAG_EXTERNAL_RERANKER_API_KEY is not None
-        else config.RAG_EXTERNAL_RERANKER_API_KEY
-    )
-
     config.RAG_EXTERNAL_RERANKER_TIMEOUT = (
         form_data.RAG_EXTERNAL_RERANKER_TIMEOUT
         if form_data.RAG_EXTERNAL_RERANKER_TIMEOUT is not None
@@ -1418,7 +1406,6 @@ async def update_rag_config(request: Request, form_data: ConfigForm, user=Depend
         'RAG_RERANKING_MODEL': config.RAG_RERANKING_MODEL,
         'RAG_RERANKING_ENGINE': config.RAG_RERANKING_ENGINE,
         'RAG_EXTERNAL_RERANKER_URL': config.RAG_EXTERNAL_RERANKER_URL,
-        'RAG_EXTERNAL_RERANKER_API_KEY': config.RAG_EXTERNAL_RERANKER_API_KEY,
         'RAG_EXTERNAL_RERANKER_TIMEOUT': config.RAG_EXTERNAL_RERANKER_TIMEOUT,
         # Chunking settings
         'TEXT_SPLITTER': config.TEXT_SPLITTER,
diff --git a/src/lib/components/admin/Settings/Documents.svelte b/src/lib/components/admin/Settings/Documents.svelte
index 3f2911f..ec2de14 100644
--- a/src/lib/components/admin/Settings/Documents.svelte
+++ b/src/lib/components/admin/Settings/Documents.svelte
@@ -52,7 +52,6 @@
 	let rerankingModel = '';
 
 	let OpenAIUrl = '';
-	let OpenAIKey = '';
 
 	let AzureOpenAIUrl = '';
 	let AzureOpenAIKey = '';
@@ -132,7 +131,6 @@
 				url: OllamaUrl
 			},
 			openai_config: {
-				key: OpenAIKey,
 				url: OpenAIUrl
 			},
 			azure_openai_config: {
@@ -252,8 +250,10 @@
 			}
 		}
 
+		const ragConfigUpdate = { ...RAGConfig };
+		delete ragConfigUpdate.RAG_EXTERNAL_RERANKER_API_KEY;
 		const res = await updateRAGConfig(localStorage.token, {
-			...RAGConfig,
+			...ragConfigUpdate,
 			// Convert null (from cleared number inputs) to empty string so the backend
 			// can distinguish "clear this field" from "don't change this field"
 			FILE_MAX_SIZE: RAGConfig.FILE_MAX_SIZE ?? '',
@@ -299,7 +299,6 @@
 			ENABLE_ASYNC_EMBEDDING = embeddingConfig.ENABLE_ASYNC_EMBEDDING ?? true;
 			RAG_EMBEDDING_CONCURRENT_REQUESTS = embeddingConfig.RAG_EMBEDDING_CONCURRENT_REQUESTS ?? 0;
 
-			OpenAIKey = embeddingConfig.openai_config.key;
 			OpenAIUrl = embeddingConfig.openai_config.url;
 
 			OllamaKey = embeddingConfig.ollama_config.key;
@@ -994,14 +993,13 @@
 							</AdminSettingField>
 							<AdminSettingField
 								label={$i18n.t('API Key')}
-								description={$i18n.t('API key for embedding requests.')}
+								description={$i18n.t(
+									'Managed externally by the service. Restart the service after rotating it.'
+								)}
 							>
-								<SensitiveInput
-									variant="settings"
-									placeholder={$i18n.t('API Key')}
-									bind:value={OpenAIKey}
-									required={false}
-								/>
+								<p class="text-xs text-gray-500 dark:text-gray-400">
+									{$i18n.t('Managed externally by the service')}
+								</p>
 							</AdminSettingField>
 						</div>
 					{:else if RAG_EMBEDDING_ENGINE === 'ollama'}
@@ -1231,14 +1229,13 @@
 									</AdminSettingField>
 									<AdminSettingField
 										label={$i18n.t('API Key')}
-										description={$i18n.t('API key sent to the external reranker.')}
+										description={$i18n.t(
+											'Managed externally by the service. Restart the service after rotating it.'
+										)}
 									>
-										<SensitiveInput
-											variant="settings"
-											placeholder={$i18n.t('API Key')}
-											bind:value={RAGConfig.RAG_EXTERNAL_RERANKER_API_KEY}
-											required={false}
-										/>
+										<p class="text-xs text-gray-500 dark:text-gray-400">
+											{$i18n.t('Managed externally by the service')}
+										</p>
 									</AdminSettingField>
 								</div>
 							{/if}
diff --git a/src/lib/apis/retrieval/index.ts b/src/lib/apis/retrieval/index.ts
index fc5a7e8..81ca36e 100644
--- a/src/lib/apis/retrieval/index.ts
+++ b/src/lib/apis/retrieval/index.ts
@@ -184,7 +184,6 @@ export const getEmbeddingConfig = async (token: string) => {
 };
 
 type OpenAIConfigForm = {
-	key: string;
 	url: string;
 };
 
