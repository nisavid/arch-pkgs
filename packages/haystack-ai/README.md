# haystack-ai

Arch package for the Haystack Python framework.

## Notes

- package name: `python-haystack-ai`
- intended to be used alongside `hayhooks` for a system-managed local service
- this package installs the Python framework only; it does not ship a service unit by itself
